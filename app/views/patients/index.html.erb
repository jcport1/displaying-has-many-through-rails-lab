<h2> All Patients </h2>

<ul>
<% @patients.each do |p| %>
    <li>Name: <%= link_to p.name, patient_path(p) %><br> Number of Appointments: <%= p.appointments.count %></li>
    <br>
    <% end %>
    </ul>